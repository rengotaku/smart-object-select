# wt の dev サーバー定義。`wt serve` / Web の「起動」ボタンで使われる。
# service には割当ブロックの base+i が割り当てられ、コマンド内の ${port} に置換される。

[[services]]
name = "web"
cmd = "npm run dev -- --port ${port} --strictPort"
domain = true
